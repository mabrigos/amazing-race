{
  "name": "mirror-maze",
  "version": "1.0.0",
  "description": "Recreate the design from screenshots",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}