// Adds two numbers. If inputs are strings, it concatenates them.
function add(a, b) {
  return a + b;
}

// Multiplies two numbers.
function multiply(x, y) {
  return x * y;
}

module.exports = { add, multiply };